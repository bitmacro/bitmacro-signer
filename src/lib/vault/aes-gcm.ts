/**
 * Intenção: cifrar e decifrar dados sensíveis com AES-GCM via WebCrypto (`crypto.subtle`) no browser;
 * no servidor, só manipular blobs já cifrados (excepto RAM com TTL durante sessão activa — ver módulo `session`).
 */
export {};
